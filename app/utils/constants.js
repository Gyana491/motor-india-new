// Constants for API URLs and other configuration

// Backend URL
export const BACKEND = process.env.BACKEND || 'https://cdn.motorindia.in';

// WordPress API host
export const WP_API_HOST = process.env.WP_API_HOST || 'cdn.motorindia.in';

// Public backend URL (available on client-side)
export const NEXT_PUBLIC_BACKEND = process.env.NEXT_PUBLIC_BACKEND || 'https://cdn.motorindia.in';